#include "main.h"

/**
 * _strdup - duplicate string in new allocated space
 * 
 * @str: string to duplicate
 * Returns: a pointer to newly allocated space in memory for duplicated string
 */

char *_strdup(char *str)
{
    char *str_copy;
    size_str = malloc(str);
        if (size_str == NULL)
        {
            return (NULL);
        }




}